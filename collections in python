dictonary:
key value pair
empty_dict_1 = {}
empty_dict_2 = dict() # another way of creating empty dict
assert empty_dict_1 == empty_dict_2 #both are same

#init from a sequence of tuple pairs, useful in many cases.
    dict_2 = dict([(1, "one"), (2, "two")])
    assert "one" == dict_2[1]
    assert "two" == dict_2[2]

set:
 empty_set_wrong = {}  #curly braces are used for both sets and dicts, so how do you disambiguate?
    assert False == isinstance(empty_set_wrong, set)

    empty_set = set()
